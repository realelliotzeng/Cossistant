When writing `x-announcement`:

- Write one plain-text X post only. No thread.
- Keep it concise, specific, and product-literate.
- The full post, including `{{CHANGELOG_URL}}`, must stay within `{{X_POST_CHAR_LIMIT}}` characters.
- Include the changelog URL `{{CHANGELOG_URL}}`.
- Avoid filler adjectives, vague startup language, and hashtags unless clearly necessary. Prefer none.
- Lead with the most interesting widget or developer-facing improvement.
